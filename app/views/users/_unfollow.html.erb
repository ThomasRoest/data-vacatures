<%= form_for(current_user.relationships.find_by(followed_id: @user),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit (t :users_show_unfollow), class: "btn btn-large btn-margin" %>
<% end %>